extends ../layouts/nav-bar.pug

block content
  if errors
    each error in errors  
      .alert.alert-danger=error

  ul
  each transaction in transactions
    li
      span=transaction.transId
      a(href="/transactions/"+ transaction.transId +"/complete") Hoàn thành